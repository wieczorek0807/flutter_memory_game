part of 'values.dart';

abstract class AppPadding {
  static const gameBoard = EdgeInsets.symmetric(
    vertical: 10,
    horizontal: 10,
  );

  static const gameCard = EdgeInsets.all(8);
  static const scoreBoard = EdgeInsets.symmetric(vertical: 1.0, horizontal: 10.0);
}
